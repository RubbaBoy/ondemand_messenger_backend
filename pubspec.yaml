name: ondemand_messenger_backend
description: A program to send messages with the ondemand service
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  webdriver: any
  args: ^1.5.2
  process_run: ^0.11.2+8
  image: ^2.1.4
  http: ^0.13.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
